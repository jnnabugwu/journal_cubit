import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';

// class SettingsPage extends StatelessWidget {
//   const SettingsPage({super.key});
// //top need entrylist bloc to show journals
// //two buttons wipe journals button wipe account button (will also wipe journal)
// //An are you sure button and only works if you type in delete. 
//   @override
// //   Widget build(BuildContext context) {
// //     return BlocBuilder<EntryListBloc,EntryListState>(
// //       builder: (context, entryListState)){
// //       return Scaffold(
// //         appBar: AppBar(title: Text(
// //           'Number of journals: ${entryListState.}'
// //         ),
// //          actions: [],),
// //       );
// //     };
// //   }
// // }}